[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
piper-rs = "0.2" # Adjust version as needed
tower-http = { version = "0.6", features = ["cors"] } # For browser cross-origin requests
serde = { version = "1.0", features = ["derive"] }
